# ============================================================
# Enterprise RAG Knowledge Base - CI/Dev Dependencies (Minimal)
# ============================================================
# 仅包含 lint + 单元测试所需的最小依赖集。
# 排除:
# - 重型包 (torch/sentence-transformers/paddleocr/paddlepaddle/PyMuPDF/
#   unstructured/pandas/pillow/openpyxl): 代码中懒加载 + try/except
# - langchain 全栈: 代码中未使用 (CypherChain 为自研实现)
# - flower: Celery 监控 UI, 测试不需要
# 生产部署请使用 requirements.txt (完整依赖)。

# ======================== Web 框架 ========================
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sse-starlette==2.1.3

# ======================== 配置与校验 ========================
pydantic==2.10.4
pydantic-settings==2.7.0

# ======================== 数据库 ========================
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# ======================== 缓存与限流 ========================
redis==5.2.1

# ======================== 异步任务 ========================
celery[rabbitmq]==5.4.0

# ======================== 向量库 (try/except 降级) ========================
pymilvus==2.5.3

# ======================== 图数据库 (try/except 降级) ========================
neo4j==5.27.0

# ======================== 关键词检索 ========================
rank-bm25==0.2.2
jieba==0.42.1

# ======================== 认证与安全 ========================
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt 3.2.2: 与 passlib 兼容性最佳 (4.x 存在 verify 边界问题)
bcrypt==3.2.2

# ======================== 监控 ========================
prometheus-client==0.21.1
prometheus-fastapi-instrumentator==7.0.0

# ======================== HTTP 客户端 ========================
httpx==0.28.1
aiohttp==3.11.10

# ======================== 工具 ========================
loguru==0.7.3
python-dotenv==1.0.1
tenacity==9.0.0
orjson==3.10.12
numpy==1.26.4

# ======================== 测试 ========================
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
# aiosqlite: 测试用 SQLite 异步驱动 (conftest.py 内存库)
aiosqlite==0.20.0

# ======================== Lint ========================
ruff
