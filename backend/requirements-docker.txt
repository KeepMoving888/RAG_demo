# ============================================================
# Enterprise RAG Knowledge Base - Docker 构建依赖
# ============================================================
# 用于 Docker 镜像构建 (CI + 生产)。
# 设计原则:
# 1. 包含核心 RAG 功能所需依赖 (向量检索 + BM25 + 图谱)
# 2. torch 使用 CPU 版本 (体积 ~200MB, vs GPU ~800MB)
# 3. 排除 paddleocr/paddlepaddle (OCR 可选, 代码中 try/except 降级)
# 4. 排除 unstructured/pandas/pillow/openpyxl (文档解析可选)
# 5. 排除 langchain (代码中未使用, CypherChain 为自研实现)
# 生产环境如需 OCR/文档解析能力, 在此文件追加对应包后重建镜像。

# ======================== Web 框架 ========================
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sse-starlette==2.1.3

# ======================== 配置与校验 ========================
pydantic==2.10.4
pydantic-settings==2.7.0

# ======================== 数据库 ========================
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# ======================== 缓存与限流 ========================
redis==5.2.1

# ======================== 异步任务 ========================
celery[rabbitmq]==5.4.0

# ======================== 向量库 ========================
pymilvus==2.5.3

# ======================== 图数据库 ========================
neo4j==5.27.0

# ======================== 嵌入与重排 (CPU) ========================
sentence-transformers==3.3.1
# torch CPU 版本 (体积 ~200MB, vs GPU 版本 ~800MB, 适合容器化部署)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

# ======================== 关键词检索 ========================
rank-bm25==0.2.2
jieba==0.42.1

# ======================== 文档解析 (PDF 基础) ========================
PyMuPDF==1.25.2
python-docx==1.1.2

# ======================== 认证与安全 ========================
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# ======================== 监控 ========================
prometheus-client==0.21.1
prometheus-fastapi-instrumentator==7.0.0

# ======================== HTTP 客户端 ========================
httpx==0.28.1
aiohttp==3.11.10

# ======================== 工具 ========================
loguru==0.7.3
python-dotenv==1.0.1
tenacity==9.0.0
orjson==3.10.12
numpy==1.26.4
